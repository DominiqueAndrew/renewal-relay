{
  "name": "renewal-relay",
  "version": "0.1.0",
  "private": true,
  "description": "An autonomous renewal operations agent built for the All Things Agentic Hackathon.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "eval": "node eval/run-evaluation.js",
    "test": "node --test test/*.test.js",
    "check": "node --check src/server.js && node --check src/ai/gemini-adapter.js && node --check src/agent/renewal-agent.js && node --check public/run-state.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/firestore": "9.0.0",
    "@google/genai": "2.19.0"
  }
}
