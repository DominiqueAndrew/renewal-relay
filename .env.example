# Optional for live Gemini extraction. Without it, the synthetic demo adapter is used.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash

# Optional Firestore persistence when running with a Cloud Run service account.
GOOGLE_CLOUD_PROJECT=
FIRESTORE_ENABLED=true

# The server listens on 8080 by default.
PORT=8080
